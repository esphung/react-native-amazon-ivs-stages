pre-commit:
  commands:
    eslint:
      run: yarn lint
    type-check:
      run: yarn typecheck
    prepare:
      run: yarn prepare
    pre-commit:
      run: yarn precommit
commit-msg:
  commands:
    lint-commit-msg:
      run: npx commitlint --edit
